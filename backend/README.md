# LactoVision Backend – Phase 1

FastAPI backend with MongoDB connectivity.

Start:

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
uvicorn app.main:app --reload --port 8000
```
