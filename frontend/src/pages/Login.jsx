import {useState} from "react";import {Link,useNavigate} from "react-router-dom";import {useAuth} from "../context/AuthContext";
export default function Login(){const {login}=useAuth();const nav=useNavigate();const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);setError("");try{await login({email,password});nav("/",{replace:true})}catch(x){setError(x?.response?.data?.detail||"Login failed")}finally{setBusy(false)}}return <div className="min-h-screen flex items-center justify-center bg-slate-50 p-4"><form onSubmit={submit} className="w-full max-w-md bg-white border rounded-3xl p-8 shadow-sm"><b className="text-emerald-700">LactoVision</b><h1 className="text-3xl font-bold mt-2">Sign in</h1>{error&&<p className="mt-4 bg-red-50 text-red-700 p-3 rounded-xl text-sm">{error}</p>}<input className="w-full border rounded-xl p-3 mt-6" type="email" placeholder="Email" required value={email} onChange={e=>setEmail(e.target.value)}/><input className="w-full border rounded-xl p-3 mt-3" type="password" placeholder="Password" required value={password} onChange={e=>setPassword(e.target.value)}/><button disabled={busy} className="w-full mt-4 bg-emerald-600 text-white rounded-xl p-3 font-semibold">{busy?"Signing in...":"Sign in"}</button><p className="text-sm text-center mt-5">New user? <Link className="text-emerald-700 font-semibold" to="/register">Create account</Link></p></form></div>}
