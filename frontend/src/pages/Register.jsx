import {useState} from "react";import {Link,useNavigate} from "react-router-dom";import {useAuth} from "../context/AuthContext";
export default function Register(){const {register}=useAuth();const nav=useNavigate();const [f,setF]=useState({full_name:"",email:"",password:"",role:"farmer"});const [error,setError]=useState("");async function submit(e){e.preventDefault();setError("");try{await register(f);nav("/",{replace:true})}catch(x){setError(x?.response?.data?.detail||"Registration failed")}}return <div className="min-h-screen flex items-center justify-center bg-slate-50 p-4"><form onSubmit={submit} className="w-full max-w-md bg-white border rounded-3xl p-8 shadow-sm"><b className="text-emerald-700">LactoVision</b><h1 className="text-3xl font-bold mt-2">Create account</h1>{error&&<p className="mt-4 bg-red-50 text-red-700 p-3 rounded-xl text-sm">{error}</p>}{[["full_name","Full name","text"],["email","Email","email"],["password","Password","password"]].map(([k,l,t])=><input key={k} className="w-full border rounded-xl p-3 mt-4" type={t} placeholder={l} required minLength={k==="password"?8:undefined} value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})}/>)}<select className="w-full border rounded-xl p-3 mt-4" value={f.role} onChange={e=>setF({...f,role:e.target.value})}><option value="farmer">Farmer</option><option value="veterinarian">Veterinarian</option><option value="admin">Admin</option></select><button className="w-full mt-4 bg-emerald-600 text-white rounded-xl p-3 font-semibold">Create account</button><p className="text-sm text-center mt-5">Already registered? <Link className="text-emerald-700 font-semibold" to="/login">Sign in</Link></p></form></div>}
